rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to all documents
    // WARNING: This is insecure and not recommended for production applications.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
